# Usability Checkpoint

Before producing a handoff, verify: can a user perform at least one complete end-to-end workflow with the tool right now? If not, identify which commits remain to reach that threshold and include them in this thread's scope before handing off.
